# 2026 数学建模 C 题 原理说明

## 1. 项目背景与目标

2026 高教社杯 C 题要求为小区微网制定购电与储能策略。仓库在 `D:\new\2026-Math-Modeling`。问题四的任务是：电价改用附件 4 的逐日波动价，**分别重算问题二和问题三**，交出 `result4-2.xlsx`、`result4-3.xlsx`，并且**不得**覆盖 `result3.xlsx`。

## 2. 核心原理

一句话：计划用的电价和结算用的电价必须拆开。S3（提交）是问题二那种「0 点看见当天日前曲线」换成附件 4；S2 是同一物理计划只换结算。策略效应是 S3−S2（省钱），不是 S3−S1。

## 3. 数学与算法基础

符号 $p_{n,t}$：第 $n$ 天时段 $t$ 的附件 4 电价。其余 $g^0,q,c,d,r,E,Q_{\alpha,t}$ 与问题三相同。

问题二：
\[
\min\sum_t p_{n,t}g_t+\lambda\xi,\qquad
\text{账单}=\sum_t p_{n,t}g_t+5p_{n,t}r_t.
\]

问题三：
\[
C_n=\sum_t\bigl[p_{n,t}g^0_t+1.5p_{n,t}(q_t-g^0_t)_+-0.5p_{n,t}(g^0_t-q_t)_++5p_{n,t}r_t\bigr].
\]

不加老化、不加弃光罚、不强迫 $E_0=E_{144}$，紧急倍数保持 5。

算法：读附件 4 得 $365\times144$ 电价；问题二沿用已标定的 $\alpha,\rho,\lambda$ 日程；问题三沿用 $\alpha=0.6$、分时段 $Q_t$、三次调整；1 月预热，2–12 月计费。

## 4. 实现架构

| 文件 | 作用 |
|---|---|
| `03_代码/p2_microgrid.py` | 问题二预测 + 日前 MILP + 实时执行 |
| `03_代码/p3_microgrid.py` / `p3_backtest.py` | 问题三计划/调整 LP + 因果回测（`price.ndim==2` 时用当天一行） |
| `03_代码/p4_export.py` | 附件 4 重算、导出、作图，禁止写 `result3.xlsx` |
| `03_代码/test_p4.py` | 电价切片、费用口径、导出路径、冻结日程 |

## 5. 关键实现细节

- `day_price` / `price_row`：一维=附件 1，二维=附件 4 当天一行。
- 问题二参数**冻结**为附件 1 下的标定日志（2/1、3/15、…、11/22），不按波动电价重标定。
- 费用一律用**当天附件 4** 复算；导出填表与回测同一套数字。
- 运行前后比对 `result3.xlsx` 的 mtime/size，被改写则报错。

## 6. 实验结果与验证

附件 4：最小 0.0076，最大 1.7936 元/kWh。

| | 固定电价（附件 1） | 波动电价（附件 4） |
|---|---:|---:|
| 问题二 | 1402.16 万 | **1469.09 万**（计划 1397.60 + 紧急 71.49） |
| 问题三 | 1338.48 万 | **1401.64 万**（合同 1369.46 + 紧急 32.17） |

问题三相对问题二仍少约 67.45 万。问题二/三校验均为 0 条。`result3.xlsx` 调整购电费列合计仍为 13,082,057.07，对应总费用 13,384,828.87。图：`04_图/p4_fig_price_buy.png`、`p4_fig_cumcost.png`、`p4_fig_emg_highprice.png`。

## 7. 与其他任务的关联

问题一是单日确定性 MILP；问题二加入预测误差与 5 倍紧急；问题三加入预报与合同调整。问题四只替换电价系数，用来检验“同一套因果策略换电价是否可复现”。

## 8. 在竞赛答卷中的意义

评阅要看的是可复现，而不是另写一套随机规划。把电价当已知波动、净负荷当未知，口径与题面“根据附件 4 重新计算问题 2 和问题 3”一致。

## 9. 潜在改进方向

- 论文中问题二、三的完整章节仍待补入（当前分支正文以问题一为主，问题四已独立成节）。
- 若允许按附件 4 重标定 $\alpha$，问题二费用可能与冻结日程有约千元级差异，但会破坏“同一套策略换电价”的对照。
