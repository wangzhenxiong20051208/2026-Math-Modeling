# python
__pycache__/
*.py[cod]
.ipynb_checkpoints/
.venv/

# latex junk
*.aux
*.log
*.out
*.toc
*.synctex.gz
*.fls
*.fdb_latexmk
# 并发编译防冲突的产物：xelatex -jobname=_final（与 main.pdf 同内容，不入库）
_final.pdf
_final.aux
_final.log
_final.out
# 页数核算用的探针构建（xelatex -jobname=_probe，仅用于 pdftotext 数页）
_probe.pdf
_probe.aux
_probe.log
_probe.out

# 运行前的临时快照目录，内容为旧结果，不入库
.backup_pre_refund/

# --refund-main / --refund 的分阶段中间结果（含一份约 25 MB 的明细副本），不入库
.refund_stage/

# 问题三敏感性分析运行时自动生成的口径变体源码，由 p3_analysis.py 复现，不入库
_p3_variants/

# excel lock / temp
~$*
*.tmp

# os
Thumbs.db
Desktop.ini
.DS_Store
