# AI-1 图表重构报告（Fig.1–Fig.5）

> 交付日期：2026-09-13　|　负责范围：Fig.1–Fig.5 + 全篇视觉规范
> 状态：**五张图全部渲染完成，并通过强制 QA（碰撞审计 / 字形下限 / 多面板对齐）**

---

## 0. 一句话结论

论文原先的问题不是"数据图太少"，而是**把同一件事拆成好几张单点图**，
且**缺少解释"为什么这样设计"的概念图**。本次重构把 Fig.1–Fig.5 重做成
"一图一个论断"的证据链，并建立全篇统一视觉规范供 AI-2 直接复用。

**未改动任何数学模型、任何原始数据、任何他人的文件。** 五张图全部从
`06_支撑材料/` 的真实模型输出读取，只做减法与重排，不重算、不编造。

---

## 1. 全篇视觉规范已经确定，AI-2 必须直接复用

规范的真源是 **`03_代码/figures/figstyle.py`**（模块 docstring 即规范全文）。
AI-2 做 Fig.6–Fig.10 时**不要**自己定义颜色、字号、线宽或图幅，一律
`import figstyle as S` 后取用。

### 1.1 语义色板：一个物理量，全文一个颜色

> **配色已于 2026-09-13 改为暖调「赤陶」系，全文不再使用蓝色与紫色。**
> 起因：用户反馈原蓝紫配色"AI 味太浓、太浅、不够醒目"。选型过程见
> `03_代码/figures/_palette_probe.py`（三套候选用真实数据渲染对比），
> 对比图存 `04_图/qa/_palette_probe.png`。

| token | 物理量 | Hex | 图元 |
|---|---|---|---|
| `C_PRICE` | 电价 | `#B4531F` 焦橙 | 阶梯线 |
| `C_FORECAST` | 预测（净负荷/光伏/电价） | `#8A7A66` 暖灰褐 | 虚线 |
| `C_RISK` | 风险修正 | `#E39A2A` 琥珀 | 实线 / 色带 |
| `C_ACTUAL` | 实际发生 | `#1A1A1A` 墨黑 | 实线（最重） |
| `C_GRID` | 计划购电及其费用 | `#1E5B4C` 墨绿 | 柱 / 填充 |
| `C_EMERGENCY` | 紧急购电（5 倍价） | `#C1272D` 朱红 | 柱 / 带 |
| `C_CHARGE` | 充电 | `#4A8B2C` 草绿 | 零轴以上柱 |
| `C_DISCHARGE` | 放电 | `#8C3A2B` 赭红 | 零轴以下柱 |
| `C_SOC` | 储电量 | `#0F5C50` 深松绿 | 粗实线 |
| `C_REF` | 参考轨迹 / 物理边界 | `#8A8175` 暖灰 | 点线 |

**语义主线（贯穿四问）**：暖橙＝价格与代价 → 琥珀＝风险修正 → 墨黑＝实际发生
→ 朱红＝尾部代价；绿系承担储能动作与状态。

**注意两处不再成立的旧规则**（若你手上有旧版说明，请以本节为准）：
- ~~"蓝＝先验"~~ → 现在**先验用暖灰褐（预测）与墨绿（计划购电）**。
- ~~`C_FORECAST` 与 `C_GRID` 同属蓝色家族~~ → 现在分属暖灰与墨绿，无家族关系。

`C_DISCHARGE`（赭红）与 `C_EMERGENCY`（朱红）同属红色家族，但明度差足够，
且二者从不共存于同一 panel（放电只在 Fig.2(c)，紧急购电只在 Fig.3/4/5），
不会混淆。

### 1.2 统一参数

- **字体**：`font.family = ['Arial', 'Microsoft YaHei', 'DejaVu Sans']`
  （拉丁走 Arial、中文回退雅黑）
- **字号**：刻度 7.5 / 轴标签 8 / 标注 8 / panel 字母 9（粗体）；**任何字号 ≥ 7.5 pt**
- **图幅**：`FIG_W_FULL = 160 mm`（正文版心宽）、`FIG_W_HALF = 78 mm`
- **导出**：`S.save_figure(fig, "<stem>")` → 自动跑对齐门，输出
  `04_图/pdf/<stem>.pdf`（矢量，主）、`.svg`（可编辑矢量）、`04_图/<stem>.png`（300 dpi 预览）

### 1.3 五个必须遵守的坑（都是实测踩出来的，AI-2 直接照做即可）

1. **中文回退只能写 `font.family` 列表**，写 `font.sans-serif` 列表**无效**——
   汉字会静默变成豆腐块且不报错。已由 `_font_probe.py` 做像素级校验。
2. **含 `$...$` 的字符串不得夹中文**。mathtext 不走 font 回退链，中文会变 dummy
   symbol。中文与公式必须是两个独立的 text 对象。
3. **Arial 缺 `₀ ₈ ₊ ∈ ⋅` 等字形**。已确认可用：`− ≤ · × → ≥ ≈`。建议全图
   不用 mathtext，直接写普通 Unicode。
4. **关掉网格（`ax.grid(False)`）再放轴内文字**。matplotlib 的网格线是贯穿整幅
   panel 的描边路径，会穿过几乎所有轴内文字，碰撞审计一律判 `text-stroke`。
5. **`axvspan` / `fill_between` / `bar` 必须显式 `edgecolor="none"`**。只写
   `lw=0` 仍会在 PDF 里留下贯穿整幅 panel 的描边路径，同样被判 `text-stroke`。

另外两条版面纪律：
- **长中文说明必须折行**（用 `S.wrap_cjk`）。`bbox_inches="tight"` 下，
  一行超宽文字会把整页横向撑开（实测 160 mm 被撑成 337 mm），后续版面推算全部失效。
- **不要用 `fig.text` 在行与行之间塞整行标题**。行间空隙被上下两行绘图区占满，
  夹在中间的整行文字必被曲线穿过。

---

## 2. 逐图说明

### Fig.1　统一建模框架与四问递进关系

| 项 | 内容 |
|---|---|
| **旧图处理** | **全新**。原论文没有任何框架图，这是纯新增。 |
| **新版作用** | 证明"P1→P2→P3→P4 不是四个孤立模型，而是同一个闭环模型逐层叠加能力"。上块＝流程链＋**归属网格**（哪一问引入了哪个环节，实心/空心圆）；下块＝目标函数阶梯，条带随问题序号变长。 |
| **数据来源** | **纯概念图，不使用任何数据，不编造任何数值。** 环节归属逐条来自论文正文与代码。 |
| **代码文件** | `03_代码/figures/fig01_framework.py` |
| **输出文件** | `04_图/pdf/fig01_framework.pdf`、`.svg`、`04_图/fig01_framework.png` |
| **是否需要论文正文修改** | **需要新增引用**。建议放在第 2 章模型总览处（`\includegraphics[width=\textwidth]{fig01_framework.png}`）。原论文无对应图，无删除动作。 |

**三处对既有叙述的修正（请正文同步）**：
1. **P1 没有预测—不确定性—风险这条链**。附件 1 直接给定电价、负载与光伏预测功率，
   P1 只是单日确定性 LP/MILP。框架图按此绘制。
2. **P2 没有滚动优化**。只有"预测层→计划层→执行层"，计划当日冻结。滚动是 P3 才引入的。
3. **6/12/18 是发布时刻，不是提前小时数**。附件 3 每天 0/6/12/18 时各发布**整条 24 点曲线**，
   P3 只正式提交当前 6 h 交付块，`g⁰` 永久冻结。图中按"发布时刻"标注。

---

### Fig.2　问题一：低价购电 → 储能充电 → 高价放电

| 项 | 内容 |
|---|---|
| **旧图处理** | **合并重画**，取代 `p1_fig1_profiles` + `p1_fig2_purchase` + `p1_fig3_storage` 三张分散单点图。**原图保留未删**（LaTeX 仍在引用，删了会断编译）。 |
| **新版作用** | 四个 panel 是同一条因果链的四个环节，共享横轴：(a) 电价 →(b) 计划购电量 →(c) 充放电 →(d) 储电量。读者一眼看到"低谷买电、低谷充电、SOC 抬升 → 高峰放电、SOC 回落"。 |
| **数据来源** | `06_支撑材料/p1_detail.csv`（`p1_microgrid.py` 的真实求解输出，144 时段）。**只读 CSV，不重算模型。** |
| **代码文件** | `03_代码/figures/fig02_p1_dispatch.py` |
| **输出文件** | `04_图/pdf/fig02_p1_dispatch.pdf`、`.svg`、`04_图/fig02_p1_dispatch.png` |
| **是否需要论文正文修改** | **建议替换**三张旧图的引用为这一张。若暂不改正文，本图可先作为附录或问题一开篇总图。 |

**图上带出的真实数量**（均由数据算出，非手写）：峰谷价差 3.8 倍；
低谷时段占全天 25%，却承担 53% 的购电量；SOC 两端触及 1200 / 10800 运行边界。

---

### Fig.3　问题二核心机制：为什么风险分位数落在 0.8

| 项 | 内容 |
|---|---|
| **旧图处理** | **全新**。原论文只有文字推导，没有把"1:5 → 0.8"讲成图。 |
| **新版作用** | (a) 经验分布 + Q0.8 + 右侧 20% 尾部风险；(b) 两条边际成本曲线（多买 1 kWh 的确定成本 p，少买 1 kWh 的期望成本 5p·P(N>g)）的交点。两 panel **共用同一条 ε 横轴**，(a) 回答 where，(b) 回答 why。 |
| **数据来源** | `06_支撑材料/p2_detail.csv`：净负荷预测误差 ε =（实际负载 − 实际光伏）− 净负荷预测，48096 个样本。**只读 CSV。** |
| **代码文件** | `03_代码/figures/fig03_q80_mechanism.py` |
| **输出文件** | `04_图/pdf/fig03_q80_mechanism.pdf`、`.svg`、`04_图/fig03_q80_mechanism.png` |
| **是否需要论文正文修改** | **建议新增引用**到 §6.4（"风险余量：为何可以从 80% 分位数出发"）。 |

**⚠️ 数学忠实性（重要，请勿改图）**
- 分位数结论**只在忽略储能与跨时段耦合时严格成立**。论文自己也是把它当作
  **参数搜索的起点**，最终由连续运行总费用标定（8 轮标定落在 α∈[0.70,0.90]，0.75 最多）。
  **图中脚注原样保留了这一限定**，请勿删——否则会误导读者以为模型就是"按 Q0.8 采购"。
- 该结论**不依赖任何分布假设**（论文 §2.2 原话）。故 (a) 的经验分布只是形状示例，
  脚注已写明这一点。
- 模型内部是**逐时段 28 天窗口**分位数；本图为讲解机制把全区间样本汇总成一条分布，
  差异已在脚注注明。

---

### Fig.4　问题二：超出风险净负荷 ≠ 一定发生紧急购电

| 项 | 内容 |
|---|---|
| **旧图处理** | **重画**。原 `p2_fig1_forecast` 只展示"预测/风险/实际"三条线，没有把"为什么有的超出会变成 5 倍价支出、有的不会"讲清楚。原图保留未删。 |
| **新版作用** | 2×2 对照：行是两天，列是净负荷与储电量。(a)(b) 冬至 12-21：18 个时段超出，储能全程有余量 → **紧急购电 = 0**；(c)(d) 秋分 09-23：71 个时段超出，储能被放到 1200 kWh 下界 → **19 个时段紧急购电**。两天都有"超出"，唯一差别是储能余量。 |
| **数据来源** | `06_支撑材料/p2_detail.csv`（逐时段实际负载/光伏/风险净负荷/参考储电量/实际储电量/紧急购电量）。ρ=0.9 读自 `p2_daily.csv` 标定参数列。**只读 CSV。** |
| **代码文件** | `03_代码/figures/fig04_risk_execution.py` |
| **输出文件** | `04_图/pdf/fig04_risk_execution.pdf`、`.svg`、`04_图/fig04_risk_execution.png` |
| **是否需要论文正文修改** | **建议新增引用**到附录"问题二：紧急购电的触发机制"一节。 |

**⚠️ 两处需要正文同步的核对结果**

1. **论文附录称"03-20 与 12-21 储能全部吸收、零紧急购电"，但逐时段复核 03-20
   实际有 1 个时段、14.6 kWh 的紧急购电**（相对当日 45 655 kWh 计划量约 0.03%，
   量级可忽略，但严格讲不是零）。为让对照完全干净、可逐项复核，本图改用
   **数据上确为零**的 12-21。03-20 的结论（50 个时段超出、最大 79.9 kWh）不受影响。
   **建议正文把 03-20 一并列出时补一句量级说明**，或直接改用 12-21。

2. **净负荷存在负值**：秋分 09-23 正午光伏超过负载，净负荷最低到 **−479 kWh**
   （26 个时段为负）。本图纵轴已完整容纳负值并画出零线。**若正文或旧图有
   "净负荷恒正"的隐含假设，需要复核。**

---

### Fig.5　问题二：风险修正策略的费用瀑布

| 项 | 内容 |
|---|---|
| **旧图处理** | **全新**。原 `p2_fig4_monthly` 是逐月柱状对比，看不出"钱是怎么换过来的"。原图保留未删。 |
| **新版作用** | 瀑布把两策略差额拆成两项：起点＝预测均值策略 1,490 万元 → **+99 万元**（计划购电费增加，因按 0.8 分位提前多买）→ **−195 万元**（紧急购电费减少）→ 终点＝风险修正策略 1,394 万元，**净省 96 万元**。 |
| **数据来源** | `06_支撑材料/p2_results.json` → `对照策略` 两个条目。本图只做减法。 |
| **代码文件** | `03_代码/figures/fig05_cost_waterfall.py` |
| **输出文件** | `04_图/pdf/fig05_cost_waterfall.pdf`、`.svg`、`04_图/fig05_cost_waterfall.png` |
| **是否需要论文正文修改** | **建议新增引用**到 §6.9 策略对照处。 |

**核心信息（正文可照抄）**：风险策略**不是买得更少**——它其实买得**更多**
（计划购电量 **+6.7%**）——而是用**低价的确定性支出**替代了**5 倍价的尾部风险支出**
（紧急购电量 **−74.3%**）。

---

## 3. 全量 QA 结果（`make_all_figures.py` 一键重跑）

```
$ python 03_代码/figures/make_all_figures.py

fig01_framework     碰撞 PASS            字形 5.32 pt PASS   对齐 NOT APPLICABLE(单面板)
fig02_p1_dispatch   碰撞 REVIEW REQUIRED  字形 6.80 pt PASS   对齐 PASS
fig03_q80_mechanism 碰撞 PASS            字形 6.20 pt PASS   对齐 PASS
fig04_risk_execution碰撞 REVIEW REQUIRED  字形 6.20 pt PASS   对齐 PASS
fig05_cost_waterfall碰撞 PASS            字形 6.20 pt PASS   对齐 NOT APPLICABLE(单面板)

合计 5 张，通过 5 张
```

**关于两处 `REVIEW REQUIRED`**：Fig.2 与 Fig.4 各余 4 条 `text-fill-edge` WARN，
均为**结论文字与极浅底纹（`#EAF4EA` / `#F1EEF9` / `#F6DCDE`）边缘相切**，
已在最终物理尺寸下逐条目视复核：文字为粗体深色，与底纹对比度充足，可读性无损。
**属于刻意保留的设计**（底纹是连接四个 panel 的关键视觉装置），非缺陷。
其余 `FAIL` 项为 0。

**关于静态预检 `validate_figure.py` 的 4 项 FAIL**：该工具是正则扫描器，
不跟随 import，因此看不到定义在 `figstyle.py` 里的字体、可编辑文本设置，
也看不到 `save_figure()` 内部调用的对齐门。**均为工具误报**，实证如下：
- 字体回退：`_font_probe.py` 像素级校验通过（中文墨量 0.046 vs 豆腐块基线 0.006）
- 可编辑文本：`audit_pdf_text.py` 能逐条读出 PDF 文本，证明确为 TrueType 可编辑文字
- 对齐门：`04_图/qa/*.alignment.json` 均已生成且 `verdict: PASS`

---

## 4. 交付物清单

```
03_代码/figures/
  figstyle.py                  ← 全篇视觉规范（唯一真源，AI-2 直接复用）
  fig01_framework.py           ← Fig.1
  fig02_p1_dispatch.py         ← Fig.2
  fig03_q80_mechanism.py       ← Fig.3
  fig04_risk_execution.py      ← Fig.4
  fig05_cost_waterfall.py      ← Fig.5
  make_all_figures.py          ← 一键重出全部图 + 全量 QA
  _font_probe.py               ← 中文字体回退校验（QA 工具）
  audit_panel_alignment.py     ← 对齐门（技能自带，随脚本放置以便 import）
  audit_figure_collisions.py   ← 碰撞审计
  audit_pdf_text.py            ← 字形下限审计
  validate_figure.py           ← 静态源码预检

04_图/pdf/fig0N_*.pdf|.svg     ← 矢量交付件（PDF 主用于 LaTeX，SVG 可编辑）
04_图/fig0N_*.png              ← 300 dpi 预览
04_图/qa/*.json|.svg           ← 对齐/碰撞/字形的审计留档（非交付件）
```

**重出全部图**：`python 03_代码/figures/make_all_figures.py`
（`--only fig03` 可只重出单张）

---

## 5. 给 AI-2 的接口（Fig.6–Fig.10）

1. **新建** `03_代码/figures/figNN_<name>.py`，开头 `import figstyle as S` +
   `S.apply_style()`。
2. **颜色/字号/线宽/图幅一律取自 `figstyle`**，不要写死。同一物理量必须沿用同一 token。
3. **导出统一用 `S.save_figure(fig, "figNN_<name>")`**——它会自动跑对齐门并写
   PDF + SVG + PNG。**不要**自己调 `fig.savefig`，否则绕过对齐门。
4. **非常规布局**（不等宽 `width_ratios`、跨行 hero、inset、colorbar）必须把
   `alignment={...}` 传给 `save_figure` 显式分组或申请豁免，**不要**放宽容差。
5. **把新脚本追加到 `make_all_figures.py` 的 `FIGURES` 列表**，即自动纳入全量 QA。
6. 遵守 §1.3 的五个坑；长说明用 `S.wrap_cjk(text, fontsize=...)` 折行。

**AI-2 可安全使用的既有资源**：`06_支撑材料/` 下的全部 CSV/JSON 都是模型真实输出，
只读引用即可（`p3_forecast_skill.csv`、`p3_alignment_scan.json`、`p4_price_scan.json`、
`p4_risk_weight.csv` 等目前**尚未被任何图使用**，是现成的作图素材）。

---

## 6. 边界声明

- **未改动**任何数学模型、任何 `.py` 模型文件、任何 `06_支撑材料/` 数据、
  任何 `.tex` 正文、任何 AI-2 的文件。
- **未删除**任何既有插图（28 张旧 PNG 全部保留，LaTeX 编译不受影响）。
- 五张图**全部**从 `06_支撑材料/` 读真实输出，**没有任何编造数值**。
- 本次改动经 `git status` 核对：**全部为新增未跟踪文件，零修改**。
