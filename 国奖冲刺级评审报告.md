# CUMCM 2026 C题 国奖冲刺级严格评审

> 评审立场:我是评分这份论文的评委,不是你们的队友。下面的结论全部有据可查——凡我亲手复核过的,标注了证据;凡我无法判定的,我明说需要什么才能验证。
>
> 评审日期:2026-09-12
> 评审对象:仓库 `2026-Math-Modeling`
> 证据来源:论文源码与 PDF、`03_代码/` 全部 24 个脚本、`06_支撑材料/` 全部结果文件、`04_图/` 全部 22 张图、`01_题目/format2026.doc`

---

## 一、总体判定

### 等级:**国赛二等奖水平**(按当前**提交状态**读:省奖～国二之间)

### 模拟评分(百分制)

| 维度 | 得分 | 扣分主因 |
|---|---|---|
| 问题分析 | **12**/15 | 四问链路分析扎实,P3 交付块歧义识别是真本事;但 P2 的对照策略未在同等条件下运行,分析结论被污染 |
| 模型建立 | **15**/20 | 模型结构真实、可用;但论文对模型的**文字描述与代码实现多处不符**(电量/功率、LP松弛、系数集、锚点口径) |
| 模型合理性 | **10**/15 | λ=0 冻结使储备机制退化,ρ 标定在一个**退化参考轨迹**上;论文对该机制的解读与实际不符 |
| 算法/求解 | **11**/15 | HiGHS/PuLP 用得恰当,η_cη_d=0.81<1 放松互斥的论证漂亮;但"六项/九项检查"有相当比例是重言式或凭空的零 |
| 数据分析 | **8.5**/10 | **最强项**。附件4=附件1+星期偏移的分解、附件3 的 191.51/808.67/268.02 对齐扫描,是真正的侦探工作 |
| 结果可靠性 | **5.5**/10 | 基线不同条件、检查表含空转项、超参选择泄漏评价集、46ms 不可复现 |
| 创新性 | **6**/10 | 有价值改进,非真创新;但 P4 价格结构发现与"最值钱的不是最准的"是真洞见 |
| 论文表达与规范 | **2**/5 | **提交的 PDF 有 152 处 `??`**;图题"九种/八种"自相矛盾;§5.4 叙述与自己的数据冲突 |
| **合计** | **70**/100 | |

### 如果保持现在的水平,最可能拿什么奖?

**看提交的是哪份 PDF:**

- 若提交**现在仓库里的 `main.pdf`** —— 全文 152 处 `??`,摘要页、模型建立、结果分析里到处都是。评委读三页就会形成"这队没检查过自己的论文"的印象。**省奖,甚至可能连省一都危险。**
- 若重新编译后提交 —— 底层工作是**国二偏上、够得着国一门槛**的料。

这中间的差距,是**五分钟的编译**。

### 距离国奖还差什么?

不是差模型,不是差数据,不是差数学。差三样:

1. **可复现性**:交付的 Python 脚本默认路径**跑不出论文的数字**(P2 必须加未记录的 `--core jia`;P3 头条数字根本不是被审计的那个文件产的)。
2. **诚实的检查表**:论文声称"六项/九项强制检查全部通过",但其中多项在构造上必然通过,或干脆是**不存在的检查被 `.get(k,0)` 补出来的零**。国奖评委里有人会去翻附录。
3. **对照实验的公平性**:三个基线在不同的一月参数、不同的初始储电量下运行,论文只报了期末值。

---

## 二、逐问建模逻辑评审(①–⑯)

### 问题一

| # | 项 | 判定 |
|---|---|---|
| ① | 是否理解正确 | ✅ 单日确定性、储能、弃光、日循环 |
| ② | 是否完整回答 | ✅ 购电量/费、充放电、储电量、result1.xlsx |
| ③ | 漏问/错问/偷换 | ⚠️ `result1.xlsx` 的时段标签被改写成 `0:00-0:10…`,与模板 `0:10-0:20…` 不同。论文已声明,但按模板字面读的人会把整条序列读早一格 |
| ④ | 思路合理 | ✅ |
| ⑤ | 为什么这个模型合适 | ✅ 144 段 × 单一储能 = 天然 MILP,规模小到可全局最优 |
| ⑥ | 变量/参数/目标/约束清楚 | ❌ **论文说"以电量(kWh)为决策变量单位",代码里 `g/c/d/s` 全是功率(kW)**,只在目标、递推、输出各乘一次 Δt。数值全同,但**描述与实现相反** |
| ⑦ | 隐含假设 | ⚠️ 未声明:PV 全额可用、电价已知确定、储能无退化/自放电 |
| ⑧ | 假设合理 | ✅ 题目给定 |
| ⑨ | 推导严谨 | ✅ |
| ⑩ | 看似专业实则无意义的公式 | ✅ 未发现 |
| ⑪ | 是否为套模型而建模 | ✅ 否 |
| ⑫ | 算法真能给出所声称结果 | ✅ 我核过:864 变量、289 等式、288 不等式、gap=7.28e-12、`max_t min(c_t,d_t)=0.0` 精确为零 |
| ⑬ | 参数来源可靠 | ✅ |
| ⑭ | 数据充分 | ✅ |
| ⑮ | 结果是否验证 | ⚠️ 六项复核中第 8 项(`Σd=η_cη_d Σc`)是第 5+6 项的推论,"六项独立复核"名不副实 |
| ⑯ | 有无更好方案 | — |

**必须改的三处描述错误:**

1. `main.tex:572-573` 写"去掉整数要求(令 0≤z_t≤1)后退化为普通 LP"。**代码是把 z 连同 288 条互斥约束整个删掉**(实测 LP:720 变量、289 行、无 A_ub),不是 0≤z≤1 的松弛。你实际做的是**更强的**证据,论文却写弱了。
2. `main.tex:568-569` 写"所有非零系数均取自 {0,±1,±ηc,±1/ηd}"。**假**:A_ub 含 ±5000,互斥行含 ±M。
3. `main.tex:680` 与 `moved_body.tex:189-190` 写"0:00–6:00 的夜间低谷维持在每 10 分钟 1400 kWh 上下"。**`p1_detail.csv` 显示 36 个时段里只有 6 个在 1400 附近,其余是 576.6–911.2**。这是**论文文字与自己输出的数据直接矛盾**——评委把图2 和这句话对着看就会发现。另有两处窗口标签差一格("0:00–0:40"的最大值实际落在 0:40–0:50;"9:30–14:20 共 30 个时段"实际是 29 个)。

- **当前模型 → 推荐模型**:MILP(功率变量) → **不改模型,改文字**。把 §5.2 表述改为"以功率为决策变量、能量量由 Δt 折算",把 LP 松弛那句改成"删除二元变量与互斥约束后的松弛",把夜间叙述换成 `p1_detail.csv` 的真实形态。
- **预计提升**:1.5–2 分(论文表达与规范 + 模型建立的可信度)。

---

### 问题二

| # | 项 | 判定 |
|---|---|---|
| ① | 理解正确 | ✅ |
| ② | 完整回答 | ✅ |
| ③ | 漏问/错问 | ⚠️ 题目要求"分析负载与光伏的**不确定性**",论文用**预测误差分位数**回答——正确,但未做不确定性分解(负荷侧 vs 光伏侧各贡献多少) |
| ④ | 思路合理 | ✅ 三层滚动(预测→风险修正→日前 MILP→实时执行)是本题最自然的解法 |
| ⑤ | 为什么合适 | ✅ 关键比 0.8 由 5 倍罚金一阶条件导出,与 8 次标定的 α 实测吻合——**这是全文最漂亮的一处理论-数据互证** |
| ⑥ | 变量/参数清楚 | ⚠️ 论文说误差分位数样本"不足 10 个"时降级,代码 `JIA_MIN_SAMPLES = 8`。报告范围内从不触发,但**论文写的阈值不是你跑的那个** |
| ⑦ | 隐含假设 | ❌ **未声明**:不要求日循环的前提下,跨日库存可自由动用;各策略的**初始库存相同** |
| ⑧ | 假设合理 | ⚠️ |
| ⑨ | 推导严谨 | ✅ |
| ⑩ | 无意义公式 | ✅ |
| ⑪ | 套模型 | ✅ 否 |
| ⑫ | 算法真能给出结果 | ✅ **我用投毒实验证伪了前视偏差**:把第 60–364 天全部实际值替换成垃圾,第 0–59 天的 `g,c,d,r,w,E,Ē,n_risk,cost` **逐位完全相同**,连第 31 天标定选择都一样。这是罕见的严格自证 |
| ⑬ | 参数来源 | ✅ |
| ⑭ | 数据充分 | ✅ |
| ⑮ | 结果验证 | ❌ 见下 |
| ⑯ | 更好方案 | — |

**三处国奖级硬伤:**

**🔴 (A) 交付脚本默认跑不出论文数字。**
`python 03_代码/p2_microgrid.py` 的 `argparse` 默认是 `--core hybrid`,其 warmup 网格把 `alphas=(0.80,)`、`rhos=(0.75,)` **硬性写死**,产生不了 JSON 里的 `选中: α=0.75, ρ=0.9, λ=0.00`,也产生不了 180 点候选。论文的数字**只能由 `--core jia` 分支复现**。而 `--core jia` 在论文、`03_代码/README.md`、支撑材料中**均无记载**。

> 按 `format2026.doc` 第五条:"缺少源程序、程序不能运行、**结果与论文不符**——可能被取消评奖资格"。这一条踩在线上。

**🔴 (B) 三个对照策略不在同等条件下比拼。**
`main:1369/1376` 调 `run_strategy` 时不传 `warmup`,于是回退到 `RiskParams()`(α=0.80, ρ=1.0, **λ=0.60**)跑一月,而主策略用一月选出的 (α=0.75, ρ=0.9, λ=0.00);还用 `CalConfig` 默认(每窗口重选 λ)而非 `jia_cal_config`(λ 冻结),且 `day0="cold"` 对 `"plan"`。

实测后果:**进入 2025-02-01 报告窗口时,三个策略的初始储电量分别是 主策略 2287.68、预测均值 7097.30、无储能 6000.0 kWh——约 4810 kWh 的免费库存差没被报告。** 论文只报了**期末**值(1559.7/5960.7/6000.0),而期末值不能抵消期初的不对称。

963,959 元的"节省"里,约 2838 元(0.29%)是白送的库存。量级不大,**但"同一评价区间"这句话在严格意义上是站不住的**。

**🟠 (C) λ=0 冻结,使储备机制退化为空转,而论文的解读描述的不是实际起作用的机制。**
`jia_cal_config(warmup_rp.lam)` 传进 `lam=0.00` → 每个解里 `ξ` 被钉死在 `E_TAR − E_MIN = 4800`,`Ē_143 = 1200`。没有终端价值,`Ē` 在日内被推向 `E_MAX`、日终推向 `E_MIN`:实测 **Ē = 10800 出现在 15.6% 的报告时段,= 1200 出现在 18.2%**。

由于 `R_t = E_MIN + ρ(Ē_t − E_MIN)`,**ρ=0.5 是依据一条退化轨迹标定出来的**;实际储能在 48,096 个报告时段中有 **5.5% 已经低于 R_t**,执行层因此被结构性禁止放电(其中 3.7% 的时段实际储能在 3000 kWh 以上却仍不能放)。

论文对这条机制的解释是"不主动放到当段参考电量以下,为后续时段保留更多电量"——**这不是实际在发生的事**。

- **当前模型 → 推荐模型**:保留三层架构 → **加一次 λ 消融 + 储备线有效性检验**。跑 λ ∈ {0, 0.6, 1.2} 三档,同时报告 `Ē_t` 的分布(落在上/下限的比例)与 `R_t` 约束的触发率。这样"机制有没有用"从一个**未检验的假设**变成**一张图**。
- **为什么更适合**:λ=0 本身是可辩护的(论文 1176–1181 行的论证很好),但**你现在只论证了"λ=0 没让它变差",没论证"这个机制在起作用"**。消融一做,两种情况都变成正面结果。
- **预计提升**:模型合理性 +1.5,结果可靠性 +1。

---

### 问题三

| # | 项 | 判定 |
|---|---|---|
| ① | 理解正确 | ✅ **交付块歧义是本题真正的坑,你们识别出来了** |
| ② | 完整回答 | ✅ |
| ③ | 漏问/错问 | ✅ 三量分离 `g⁰/x/r`、`G^recv=Σx+Σr`,无重复计入(我核过:`gP=20762854.173`,只在计划费出现) |
| ④ | 思路合理 | ✅ 两门槛 `[Q_0.7, Q_0.9]` "不必调整区间"是正文里最漂亮的一步 |
| ⑤ | 为什么合适 | ✅ |
| ⑥ | 变量/参数清楚 | ✅ |
| ⑦ | 隐含假设 | ❌ 未声明:评价区间不参与参数选择 |
| ⑧ | 假设合理 | ❌ **被证伪**,见下 |
| ⑨ | 推导严谨 | ✅ |
| ⑩ | 无意义公式 | ✅ |
| ⑪ | 套模型 | ✅ 否 |
| ⑫ | 算法真能给出结果 | ✅ 主结果链路(`p3_microgrid.py`+`p3_backtest.py`+`p3_analysis.py`)自洽:`12583676.62 + 804431.51 − 242417.95 + 272936.18 = 13418626.36`(论文 13,418,626.35,差 0.01 元是逐项舍入) |
| ⑬ | 参数来源 | ⚠️ 见下 |
| ⑭ | 数据充分 | ✅ 附件3 对齐扫描(191.51 vs 808.67 vs 268.02)是真功夫 |
| ⑮ | 结果验证 | ❌ 检查表有空转行 |
| ⑯ | 更好方案 | — |

**四处硬伤:**

**🔴 (A) 头条数字的来源文件对不上。**
被交付的 `p3_rolling_microgrid.py`(1488 行)在文件头声明自己产出 `p3_results.json` / `p3_detail.csv` / `p3_daily.csv` / `p3_cache/` —— **这四个路径在 `06_支撑材料/` 下全部不存在**。该文件从未完整跑过。论文 P3 数字的真实链路是 `p3_microgrid.py` → `p3_backtest.py` → `p3_analysis.py`。

更麻烦的是:**两套实现在"主口径"这个名义下定义相反**。滚动文件里 `main` = **不退款**、`refund` = 退款;而论文 `p3_section.tex:105-114` 明确写"主口径(退款)"。滚动文件的 `main` 分支结构上**不可能**产生论文主表的"调减 775,660.56 kWh / 调减退费 242,417.95 元"(它把 `x_t ≥ g⁰_t` 锁死、禁掉下调量)。

**一个评委按文件头的声明去复现,会得到与论文制度设定就不同的账本。**

**🔴 (B) 检查表里有凭空的零。**
`tab:p3-check` 九行中,"**跨日状态衔接**"报"未通过天数 = 0"——但 `p3_export.py` 全文**没有这个检查**(grep 无"衔接"),`validate_day` 只验 E₀ 参与上下界,不验 `E0[d+1] == E[d][-1]`。**这个 0 是补出来的。**

反过来,**"同时充放电"这一项是真检查**(因为 `p3_microgrid.py` 全文无 `LpBinary`,这条是唯一保证),论文却在附录把它标成"结构上恒成立"——**把做过的检验说轻了**。

**🟠 (C) 超参选择泄漏了评价区间。**
论文 `p3_section.tex:213-217` 称参数冻结后"2 月 1 日–12 月 31 日的评价区间内**不再回头修改**"。但 `p3_calibration.py:85-100` 同时算了 `best_febdec` / `jan_best_febdec` / `jan_best_lam_febdec` —— **中心点 (0.6, 0.5, 28) 与 λ=1.0 这两项,是靠 2/1–12/31 的费用比较才定下来的**。参数选择阶段已经读过评价集。

论文把这段作为外推检验如实报告了,但**同时又用它论证取中心的正确性**——严格说这是超参数层面的评价集泄漏。

**🟠 (D) 锚点口径与代码不符。**
论文 `p3_section.tex:70-77` 称"锚点取自附件2 中已经发生的**实测功率**"。产出论文数字的链路确实如此,但**被交付的滚动文件用的是上一版预报值** `F[hi-1, n, 6]`。两者都**无前视偏差**(是决策时已知的发布值),但数值上不等价(6 小时预报有误差)。

另:`p3_section.tex:177-178` 写"两个模型都是线性规划而非混合整数规划…无需二元变量"——对滚动文件为假,它用 big-M 二元互斥。

- **当前模型 → 推荐模型**:不变 → **改"主口径"命名 + 重做参数选择 + 修检查表**。
- **具体**:①把 `p3_rolling_microgrid.py` 头部改成"本文件仅供问题四复用内核,不作为问题三结果来源",并把 `main/refund` 改名为 `norefund/refund`;②参数改用 **1 月内部再切分**(前 20 天选参、后 11 天验证)后冻结,把这次外推检验降级为"事后稳健性说明";③"跨日状态衔接"行改成"未单独设判据(由逐日 E₀ 传递结构性保证)"或直接删行;"同时充放电"改标为"真实检验"。
- **预计提升**:结果可靠性 +1.5,创新性 +0.5。

---

### 问题四

| # | 项 | 判定 |
|---|---|---|
| ① | 理解正确 | ✅ |
| ② | 完整回答 | ✅ |
| ③ | 漏问/错问 | ✅ |
| ④ | 思路合理 | ✅ 决策价与结算价分离、按附件4 实际价结算 |
| ⑤ | 为什么合适 | ✅ |
| ⑥ | 变量/参数清楚 | ✅ |
| ⑦ | 隐含假设 | ✅ 边界章节主动列了四条 |
| ⑧ | 假设合理 | ✅ |
| ⑨ | 推导严谨 | ✅ **无 2.5p 重复计价**(我核过:`p4_microgrid.py:1171` 计划费基数是 `g0` 不是 `x`) |
| ⑩ | 无意义公式 | ✅ |
| ⑪ | 套模型 | ✅ 否 |
| ⑫ | 算法真能给出结果 | ✅ **P4 是我核得最放心的一问**:8 个预报组合的 12 条边际值**逐个精确复现**——单独加 18:00 = 52.28万、6:00 = 35.64万、12:00 = 28.63万;已有{6,12}再加18 = 11.71万;三版合计 = 62.05万。全中 |
| ⑬ | 参数来源 | ⚠️ 见下 |
| ⑭ | 数据充分 | ✅ 附件4=附件1+星期偏移的分解(年际均值偏差 5.15e-5、`corr(R,F)=−1.5e-5`、星期一 ~+0.053 / 周五周六 −0.137、解释 41.7% 方差)数据、图、正文**三者完全一致** |
| ⑮ | 结果验证 | ⚠️ 见下 |
| ⑯ | 更好方案 | — |

**三处硬伤:**

**🟠 (A) "三种策略各自独立标定"是假的。**
`p4_section.tex:385` 与 `p4_tables_appendix.tex:62` 都写"三种策略**各自独立标定**参数,用的是各自信息集下的历史"。但代码自己的 docstring 写的是反话:`p4_microgrid.py:2092-2097` —— "三个价格模式**共用同一组 1 月预热期参数**…固定住它,ΔJ 才只反映评价区间内价格信息之差"。

评价区间内的滚动重标定确实是逐模式独立的,所以是**半独立**。共用预热参数本身**是个好选择**(理由写得很对),但论文的绝对表述与代码矛盾。

**🟠 (B) `tab:p4-check` 有两列凭空的零 + 一个是空转的检查。**
`p4_tables.py:678-682` 用 `.get(k, 0)` 补齐:4-3 的"计划冻结"、4-2 的"调整权限"显示为 0,实际是**该分支不存在这个检查键**。

更关键的是名为"**信息边界**"的检查(`p4_microgrid.py:1531-1534`):它只验价格/风险净负荷有限且为正,**根本不验任何发布时刻**——而论文称它覆盖"发布时刻均不晚于求解时刻"。

**"下调量恒为零"的验证是循环论证**:先 `ub[iB:iB+m] = 0.0` 禁掉下调量,再检查"有没有下调量"。恒真,不可能失败。另外两处判断(`S == ()` 时循环体永不进入)不可达。

**🟡 (C) 声称做了没做的校验。**
`p4_section.tex:296-299` 说缓存三元组隔离"并在第 sec:p4-result 节校验"——**代码里没有任何该自检**,grep "缓存" 只命中常量、注释和 `save_cache`。

**另:** 4-3 的 (α,ρ) 在 8 个窗口里全部落在网格左下角 —— 这一点**论文自己主动承认了**"其取值不具可解释性"(line 451-452)。这个诚实度值得肯定,但评委必然会追问"那你的 4-3 主策略参数是不是根本没有意义?"

- **当前模型 → 推荐模型**:不变 → **把三处表述改成代码事实 + 补缓存隔离自检**。
- **预计提升**:结果可靠性 +1,论文表达 +0.5。

---

## 三、国奖级硬伤汇总

### A. 逻辑漏洞

| # | 问题 | 位置 | 级别 |
|---|---|---|---|
| A1 | **提交的 PDF 有 152 处 `??`** —— 交叉引用全部失效。我已实测:干净重编译 → **0 处**,页数仍 144 | `main.pdf` | 🔴 |
| A2 | §5.4/§5.5.1 叙述与自己的 `p1_detail.csv` 矛盾 | `main.tex:680` | 🟠 |
| A3 | 图 p3_fig4 内嵌标题"**九种**预报",图下 LaTeX 标题与正文写"**八种**",横轴实际 **8 根柱** | `p3_fig4_combos.png` | 🟠 |
| A4 | `tab:p3-check`"跨日状态衔接"报 0 但该检查不存在 | `p3_tables_appendix.tex:145-155` | 🟠 |
| A5 | `tab:p4-check` 两列零是 `.get(k,0)` 补出来的 | `p4_tables.py:678-682` | 🟠 |
| A6 | 附录称"365 天全部成立",循环是 `range(1, len(records))` = **364 次** | `p2_microgrid.py:1255` | 🟡 |
| A7 | 2.3e-13 残差是 **365 天**口径,却与 **334 天**的费用并列 | 论文 | 🟡 |

### B. 数学问题

无。我逐项核过目标函数、约束、递推、关键比 0.8 的一阶条件、投标价加权分位数、12 条边际值 —— **数学层面没有发现错误。** 这是这份论文最硬的部分。

唯一需要说明的是:`p2_microgrid.py:255` 用 `np.quantile` 默认**线性插值**,而 P4 的加权分位数自检用 `inverted_cdf`。两个问题混用了两种分位数约定,论文未提。

### C. 建模问题

| # | 问题 | 级别 |
|---|---|---|
| C1 | λ=0 冻结 → ξ 钉死在 4800 → Ē 在 15.6% 时段触上限、18.2% 触下限 → ρ 标定在退化轨迹上;执行层在 5.5% 时段被结构性禁止放电,而论文的解读描述的不是这个机制 | 🔴 |
| C2 | 三个对照策略初始储电量不同(2287.68 / 7097.30 / 6000.0),只报期末值 | 🟠 |
| C3 | 论文说决策变量是"电量(kWh)",代码是**功率(kW)** | 🟠 |
| C4 | 论文说 LP 松弛是"令 0≤z_t≤1",代码是**删掉 z 与 288 条互斥约束** | 🟠 |
| C5 | 论文说"所有非零系数均取自 {0,±1,±ηc,±1/ηd}",A_ub 含 ±5000 | 🟡 |
| C6 | P3 锚点:论文"实测功率" vs 滚动文件"上一版预报" | 🟠 |

### D. 算法问题

| # | 问题 | 级别 |
|---|---|---|
| D1 | `python 03_代码/p2_microgrid.py` 默认 `--core hybrid` **跑不出论文数字**,需未记载的 `--core jia`。踩 `format2026.doc` 第五条"结果与论文不符可能被取消评奖资格" | 🔴 |
| D2 | P3 头条数字的产出文件不存在;两套 P3 实现"主口径"定义相反 | 🔴 |
| D3 | P4"三种策略各自独立标定"与代码 docstring 直接冲突 | 🟠 |
| D4 | P3 超参中心点与 λ 由评价区间费用选定("不再回头修改"不成立) | 🟠 |
| D5 | 论文称缓存隔离"已校验",代码无此自检 | 🟡 |
| D6 | P2 六项检查中 **只有第 3、6 项是独立验证**;2、4、5 是重言式;第 1 项部分是硬编码 `print("… —— 通过")` | 🟠 |
| D7 | 46 ms 求解时间**无法从交付代码复现**(我实测均值 37.4 ms,范围 37–56 ms) | 🟡 |
| D8 | 检查表声称的 2.3e-13 残差是**代数恒等式**(C/W/D/R 由同一个 b 分解定义),不是约束残差 | 🟡 |
| D9 | 1.58× / 0.888 / 8.9% / 38.5% / 1769824 / 693721 / 466267 / 19068890 等数字**不由任何交付脚本产出**,只作为注释存在于 `p2_microgrid.py:155-164`。我全部独立复现过——**数值都是对的**,是溯源缺口不是错误 | 🟡 |
| D10 | `for i, row in enumerate(s["table1"]): pass` 空转死代码 | 🟢 |

### ✅ 明确**没有**发现的问题(对抗性搜索的否定结论)

- **无前视偏差** —— P2 投毒实验逐位证伪;P3/P4 的全部预报、误差样本、价格预测、日内 κ 偏置、风险分位、参数标定均只用严格早于决策时刻的观测。
- **无 2.5p 重复计价** —— 计划费基数在 P3/P4 都是 `g⁰`。
- **无 −0.5p 符号错误** —— 且 `0 ≤ v ≤ g⁰` 显式保证。
- **无 g⁰ 重复计入** —— `G^recv = Σx + Σr`。
- **无头条数字硬编码。**
- **数据层完美** —— 所有可复算的附件数值全部精确对上。
- **P4 价格结构分析三处一致**(数据/图/正文),含 8 个组合的全部 12 条边际值。

---

## 四、创新性审查

### 分类

| 创新点 | 分类 |
|---|---|
| 附件4 = 附件1 曲线 + 星期偏移,41.7% 可解释 | **真创新**(数据层面的发现) |
| "每个预报版本的价值取决于它锁定的交付块是否难预测,而不取决于它自己的精度" | **真创新**(结论层面的洞见) |
| η_cη_d = 0.81 < 1 ⟹ P1/P3 的互斥约束可放松为纯 LP | **有价值的改进** |
| 交付块策略(每个交付时刻只正式提交当前块) | **有价值的改进** |
| 价格加权经验分位数(含零权重回退与自检) | **有价值的改进** |
| 三层滚动架构(预测→风险修正→日前 MILP→实时执行) | 常规模型组合 |
| MILP + 分位数风险 + 滚动优化 | 常见模型套用 |

**不是换皮创新。** 前两项是真东西,中间三项是扎实的工程改进。

### 当前创新点 → 如何增强 → 论文中应该怎么写

| 当前 | 如何增强 | 应该怎么写 |
|---|---|---|
| 附件4 价格结构(41.7%) | 你已经在附录承认了另 58.3% 是噪声。**但没做"这 58.3% 值多少钱"的量化** —— 用 P4 的 oracle 分支已经能算:oracle − forecast = 94,145 元(4-2)/108,477 元(4-3),**这正是不可预测部分的定价** | 把它提成一条独立结论:"**价格预测的天花板是 10.8 万元/年(0.75%)**",和"三个预报时刻值 62.05 万元"并列 |
| 预报版本边际价值 | 目前只做了 4-3(价格波动)。**建议补一张二维表**:P3(固定价)× P4(波动价)的 8 组合并排,直接显示"信息结构决定价值、价格波动不改变排序" | 摘要里"18:00 最不可替代"目前埋在 P3 段,应提为**跨问题结论** |
| LP 放松论证 | 已经很好,**但只验了"0 个时段同时充放电",没给经济解释的界**。补一句:同时充放电的边际损失率是 (1−η_cη_d) = 19%,故任何同时充放电解都被严格支配 | 放在 §5.3,作为一个**引理**而非观察 |

**注意:** 论文有把"用了多个模型""用了 AI""用了 ML"当创新的倾向——本例中没有这个问题(全文没有 ML 成分),这点是好的。

---

## 五、结果可靠性审查

| 检查项 | 状态 | 证据 |
|---|---|---|
| 模型验证 | ⚠️ 部分 | 六项/九项检查表中有重言式与空转项(D6) |
| 基线对照 | ❌ | 三个基线不同条件运行(C2) |
| 其他模型对照 | ❌ | 未做。LP vs MILP 有,但没有与其他调度范式(如规则库、MPC)对比 |
| 敏感性分析 | ⚠️ 部分 | P1 有(10 分钟 vs 4 小时粒度,24.3%);P2/P3/P4 只有参数扫描,**没有对关键假设的敏感性** |
| 误差分析 | ✅ | P3 四版本 MAE 206.6/180.0/98.6/15.9 kW;P4 MAE 0.0446 元/kWh |
| 鲁棒性分析 | ⚠️ | P3 有 α/ρ/W 网格;P4 有 β 扫描。**但都没有"预测误差放大 X 倍会怎样"的压力测试** |
| 极端情况 | ❌ | 未做 |
| 真实数据验证 | ✅ | 附件1–4 全部真实数据 |
| 交叉验证 | ⚠️ | P3 用 1 月标定/2–12 月评价——**但该划分被泄露出参**(D4) |
| 消融实验 | ❌ | **λ 消融缺失** —— 这正是 C1 的根源 |

### 最值得补的 3 个实验

> 不是一张长清单。只补三个,每一个都能把一处"看起来像硬伤"的东西变成"受控结果"。

**实验 1:对照策略的同条件重跑(最高优先级)**

把 `main:1369/1376` 的三处 `run_strategy` 调用改为传同一个 `warmup`、同一个 `CalConfig`、同一个 `day0`。重跑,然后:
- 报告**进入 2025-02-01 时的初始储电量**(三策略应相同)
- 若数字变了,**如实更新摘要里的 6.47% / 24.73%**
- 若没变,**这本身就是最强的一句辩护**

> 成本:改 6 行代码 + 一次重跑。收益:堵死"你的对照不公平"这个评委必问项,同时修掉 D1 的一半(其余部分靠补文档)。

**实验 2:λ 消融 + 储备线有效性检验**

跑 λ ∈ {0, 0.6, 1.2} 三档,对每档报告:
- 全年总费用
- `Ē_t` 落在上/下限(10800 / 1200)的时段占比 ← 这是**机制是否退化**的直接证据
- `R_t` 约束的触发率(实际储能低于 R_t 的时段占比)← 这是**机制是否起作用**的直接证据
- 被迫弃电/紧急购电的变化

> 你要展示的是:`λ=0` 时 `Ē` 退化到什么程度,以及**加上 λ 后费用是升是降**。若 λ>0 更好,直接改主策略;若 λ=0 确实更好,这张图就是"我们检验过并选择了它"的证据。**两种情况你都赢。**

**实验 3:P3 超参选择的内样本化**

把 1 月 31 天再切成 **前 20 天选参 / 后 11 天验证**,在 45 点网格上以"后 11 天费用"为准选中心,然后冻结。重跑 2–12 月。
- 若头条数字 13,418,626 基本不变 → **把最大的方法论漏洞直接抹掉**
- 若变了 → 说明原选择确实吃了评价集,新数字更可信

> 成本:复用 `p3_calibration.py`,把 `day_range=JAN` 改成两段。收益:消除 D4,把"事后外推检验"降级为稳健性说明。

**为什么只补这三个:** 它们分别对应"对照不公平""机制退化""参数泄漏"——三个**会直接改变结论可信度**的问题。其余(极端情况、压力测试、与其他范式对比)是锦上添花,边际收益低得多。

---

## 六、图表审查

### 已核验的结论(我逐张放大看过,并与 JSON 对过)

| 图 | 结论 |
|---|---|
| p4_fig3_strategy | ✅ 6 根柱 1474.1 / 1476.1 / 1464.7 / 1440.2 / 1443.5 / 1429.4,**与 JSON 逐个精确吻合** |
| p4_fig1_price_structure | ✅ 周一~周四 +0.053~0.058、周五 −0.137、周六 −0.136、周日 +0.053,**与 JSON 和正文精确一致** |
| p2_fig4_monthly | ✅ 逐月合计 = **1394.0 万**,与 `p2_daily.csv` 复算值精确吻合 |
| p3_fig1_versions | ✅ 图例含全部四版(0:00/6:00/12:00/18:00) |
| **p3_fig4_combos** | ❌ **内嵌标题写"九种预报",横轴只有 8 根柱;图下 LaTeX 标题写"八种",正文也写"八种"** |

> 补充说明:我在低分辨率拼版上曾误读 p4_fig3(1464.7 看成 1454.7)、p4_fig1(柱序看错)、p2_fig4(月值看错)。**逐一放大到原始分辨率后,这三处都证明是正确的。** 上面结论只在原始分辨率核验过之后才下。

### 逐项审查

**图的问题**

| # | 问题 | 位置 | 级别 |
|---|---|---|---|
| F1 | 内嵌标题"九种"与图注/正文"八种"矛盾 | p3_fig4 | 🟠 |
| F2 | **颜色冲突**:电价用红色实线,紧急购电量用红色斜纹柱——同一张图两种含义同色 | p2_fig2、p2_fig6 | 🟡 |
| F3 | **量程不一致**:四季节子图 y 轴范围不同(6-21 到 500,其余到 1000+),跨子图比较会被误读 | p2_fig1、p2_fig3、p3_fig1 | 🟡 |
| F4 | y 轴上限 10000 但约束上限是 10800,上限线被顶在画布边缘 | p2_fig3、p3_fig3 | 🟡 |
| F5 | 部分子图缺"上限/下限/Ē^tar"标注(仅第一子图有) | p2_fig3、p3_fig3 | 🟢 |
| F6 | **5 张 P4 图生成了却从未 `\includegraphics`**:`p4_fig2_forecast` / `p4_fig4_purchase` / `p4_fig5_storage` / `p4_fig6_combos` / `p4_fig7_daily` | `04_图/` | 🟠 |
| F7 | **图文配比失衡**:P2 有 6 张图,P4(最难、分值最高)只有 2 张 | 全文 | 🟠 |

**表的审查**

- `tab:p2-buy` / `tab:p2-storage` / `tab:p2-emg`:结构清晰,单位齐全,与代码输出精确一致 ✅
- `tab:p2-emg` 的合并事件(985.19+236.10+32.16 = 1253.45 ≈ 论文 1253.46)**是真实合并,不是凑数** ✅
- `tab:p3-check` / `tab:p4-check`:**含空转行与凭空的零**,必须修 🔴
- 表与正文的交叉引用**全部失效**(152 处 `??`)🔴

### 国奖论文最应该补哪几张图

**补 1(必要):三层滚动架构总图。**
全文最核心的方法论——"预测层 → 计划层 → 执行层"的信息流与冻结时点——目前**只有文字**。评委在速读时看不见你的架构。一张带信息集标注的流程图,把"哪些量在 0:00 冻结、哪些量逐段更新、E 如何跨日传递"画出来。**这是性价比最高的一张图。**

**补 2(必要):把 `p4_fig2_forecast` 和 `p4_fig4_purchase` 用上。**
图已经画好了,白扔了。P4 从 2 张图变成 4 张,正好补上 F7 的失衡。

**补 3(强烈建议):敏感性/消融汇总图。**
把实验 2 的 λ 消融(费用 vs λ,叠加 Ē 退化率曲线)做成一张双轴图。它同时回答"储备机制有没有用"和"你的模型稳不稳"。

**可以删的:** `p2_fig5(b) 跨日储电量轨迹` 与 `p3_fig3` 信息重复,可考虑合并。

---

## 七、结构审查(评委速读视角)

### 当前结构

```
第1页      摘要
第2–31页   正文(恰好 30 页)  ← 规则上限,零余量
第32–144页 附录(113 页)
```

**符合 `format2026.doc`:** 无目录 ✅、摘要 1 页 ✅、正文从第 4 页开始 ✅、正文 30 页 ✅(顶格)、附录不限 ✅、文件 6.25 MB < 20 MB ✅、页码连续 ✅。

### 速读体验问题

| # | 问题 | 级别 |
|---|---|---|
| S1 | **正文顶格 30 页,零余量。** 任何补充都会超页 | 🟠 |
| S2 | **核心模型出现太晚** —— 问题二的"三层滚动"是全文骨架,但到 P2 章节才展开,前面没有预告图 | 🟠 |
| S3 | **关键结论被埋** —— "最值钱的预报版本不是最准的"写在 P3 小节末尾,P4 也有呼应,但没有独立小节收口 | 🟡 |
| S4 | 模型评价公式化 —— 有(边界章节写得很好),但优点陈述偏模板 | 🟡 |
| S5 | **152 处 `??`** 分布在第 3、9–12、15–21、23–41、47、55、77–80、138、140–144 页 —— **连续 19 页都有** | 🔴 |
| S6 | **缺字符**:λ、β、τ、⁰ 在 `lmmono9-regular` 中不存在,共 128+ 处。`\texttt{}` 里写了希腊字母 | 🟠 |

> S6 需要处理:编译日志显示 `Missing character: There is no λ in font [lmmono9-regular]` 等 128 条。若这些出现在代码清单或符号说明里,PDF 中会缺字或显示为空白框。

---

## 八、摘要审查

### 结论:**这份摘要是全文最强的一环,基本不需要重设计。**

我逐句核对了摘要里的每个数字:

| 摘要数字 | 核验 |
|---|---|
| 59482.70 kWh / 35126.95 元 / 均价 0.5905 | ✅ 精确 |
| 7.3×10⁻¹² 元 | ✅ (7.275957614183426e-12) |
| 10 分钟粒度较 4 小时节省 24.3% | ✅ |
| 较"无储能"省 26.90%、较"无光伏无储能"省 60.23% | ✅ |
| 日绝对误差 3158.8 kWh/日(负载 1.73%、光伏 3.70%) | ✅ |
| 13939720.46 / 140301.24 / 755499.17 / 占 5.42% | ✅ |
| α 落在 0.70–0.90,**0.75 出现 4 次** | ✅ (0.75,0.75,0.80,0.90,0.70,0.70,0.75,0.75) |
| 较预测均值省 6.47%、较无储能省 24.73% | ✅ 算术正确(但基线不公平,见 C2) |
| 事后理想 12110855 | ✅ |
| 附件3 对齐偏差 191.51 kW | ✅ |
| 四版本 MAE 206.6/180.0/98.6/15.9 kW | ✅ |
| 13418626.35(计划 12583676.62、调整净额 562013.56、紧急 272936.18) | ✅ |
| 较对照 14977809.03 省 10.41% | ✅ (1497.8→1341.9 = 10.41%,我核过) |
| 12:00 版在已有{6,18}时省 11.44 万 | ✅ P3 口径 |
| 14740813.21 / 14402403.87 / 省 2.30% | ✅ |
| 上界 14293927.31 | ✅ |
| 三版预报合起来值 62.05 万 | ✅ (15022900.98 − 14402403.87 = 620497.11) |
| 样本外 MAE 0.0446 元/kWh | ✅ |

### 五项要件

| 要件 | 状态 |
|---|---|
| ① 什么问题 | ✅ 清楚 |
| ② 什么模型 | ✅ MILP / 三层滚动 / 交付块,写得很清楚 |
| ③ 关键结果 | ✅ **数字密度极高,这是国一摘要的特征** |
| ④ 创新 | ❌ **最弱的一环**。摘要完全没有提"附件4 = 附件1 + 星期偏移,只有 41.7% 可解释"这个真正的发现 |
| ⑤ 实际意义 | ⚠️ 弱。没有一句讲这个策略在现实里意味着什么 |

### 需要改的三处

1. **"六项强制检查全部通过"/"全年九项强制检查全部通过"** —— 这三句出现在摘要里。鉴于 D6/A4/A5,这些检查中有相当比例是重言式、空转或凭空的零。**在摘要里这么写,等于把最容易被打脸的话放在最显眼的位置。** 建议改为:"六项检查通过,其中跨日连续性与事件合并为独立复核(其余为构造性保证)"。
2. **补一句真创新。** 建议插入:"进一步分析发现,附件4 电价并非独立序列,而是附件1 目录曲线叠加一个缓慢漂移的星期偏移(周一至周四及周日 +0.053 元/kWh,周五周六 −0.137 元/kWh),该偏移解释了残差方差的 41.7%。"
3. **摘要正文提到"九项强制检查"** 与图 p3_fig4 的"九种预报"是两回事,别混淆;顺手把图题改掉。

**摘要不需要重设计。** 它的信息密度已经达到国一水平。

---

## 九、模拟评委拷问(24 问)

**【1】为什么问题一用 MILP,而不是直接线性规划?**
- 能否回答:✅ 能。互斥约束需要二元变量。
- 补充:论文已经用 η_cη_d=0.81<1 论证了松弛后最优解不变,**做得比"因为需要所以用"更好**。

**【2】你论文说决策变量是"电量(kWh)",代码里为什么是功率(kW)?**
- 能否回答:❌ **不能。**
- 怎么补:把 §5.2 的表述改成"以功率为决策变量,电量由 Δt 折算"。数值不变,但这是描述与实现必须一致的问题。

**【3】LP 松弛你怎么做的?**
- 能否回答:❌ 论文写的是"令 0≤z_t≤1",实际是**删掉 z 和 288 条互斥约束**。
- 怎么补:如实写成"删去二元变量与互斥约束后的松弛",并说明这**比 0≤z≤1 的松弛更强**,由于依然得到相同最优值,结论更硬。

**【4】0:00–6:00 的购电量真的是每 10 分钟 1400 kWh 吗?**
- 能否回答:❌ **不能。`p1_detail.csv` 里 36 个时段只有 6 个在 1400 附近。**
- 怎么补:按实际数据重写这段叙述。(该模型在充电时点上存在多个等价最优解,论文描述的是另一个顶点——这一点本身也值得说明。)

**【5】你的"六项强制检查"具体检查了什么?**
- 能否回答:⚠️ 部分。
- 怎么补:在附录明确每项是"独立复核"还是"构造性保证"。目前第 2、4、5 项在构造上必然通过,第 1 项有一部分是硬编码的 `print("… —— 通过")`。

**【6】三个对照策略是在相同条件下跑的吗?**
- 能否回答:❌ **不能。** 三个策略的一月参数、`CalConfig`、`day0` 都不同,进入窗口时初始储电量是 2287.68 / 7097.30 / 6000.0。
- 怎么补:**做实验 1**。同条件重跑,并报告期初储电量。

**【7】为什么三个策略的期末储电量不一样,你报告了吗?**
- 能否回答:✅ 报告了(`main.tex:1162-1164`)。
- 补充:但**只报了期末、没报期初**,期初才是影响可比性的量。

**【8】λ=0 意味着你的日末储备惩罚项完全没起作用,那你为什么还留着它?**
- 能否回答:✅ 能,论文 1176–1181 行的论证很好。
- 但评委追问:**"那 Ē 这条参考轨迹还有意义吗?"** ❌ 不能回答。
- 怎么补:**做实验 2**。报出 Ē 落在上下限的时段占比(实测 15.6% / 18.2%),主动承认它在 λ=0 下退化。

**【9】你的执行规则说"不主动放到参考电量以下",实际有多少时段真的被这条规则约束住了?**
- 能否回答:❌ **不能。实测 5.5%。**
- 怎么补:把它做成一张图——"R_t 约束触发率 vs λ"。这直接把一个被动的机制变成一条主动的结论。

**【10】α 的理论值是 0.8,你标定出来是 0.70–0.90,这算验证成功吗?**
- 能否回答:✅ 能,而且这是全文最漂亮的一处。

**【11】你的误差分位数样本"不足 10 个"时怎么处理?**
- 能否回答:❌ 论文写的是 10,代码是 8。且论文没提第三条分支(`k<3 → 返回全零`)。
- 怎么补:统一为 8,并补上第三条分支。报告范围内不触发,但描述必须与代码一致。

**【12】P3 头条数字 13418626.35 是哪个脚本产出的?**
- 能否回答:❌ **不能。** `p3_rolling_microgrid.py` 头部声明它产出 `p3_results.json`,该文件不存在。真实链路是 `p3_microgrid.py`+`p3_backtest.py`+`p3_analysis.py`。
- 怎么补:改文件头声明,并在论文或 README 里写明产出链。

**【13】你的 P3 主口径是"退款"还是"不退款"?**
- 能否回答:⚠️ 论文说是退款。但**被交付的滚动文件里 `main` = 不退款**。
- 怎么补:把枚举改名为 `norefund`/`refund`,并在文件头加警告注释。

**【14】附件3 的锚点你用的是实测功率还是预报值?**
- 能否回答:❌ 论文说"实测功率",滚动文件用"上一版预报的 6 小时预报值"。
- 怎么补:改成"该交付时刻最近一次发布的边界预报值",并说明两版实现都在 `anchor_source` 留痕。

**【15】你说参数冻结后"评价区间内不再回头修改",中心点是 1 月网格的最小点吗?**
- 能否回答:❌ **不是。** 中心点 (0.6, 0.5, 28) 与 λ=1.0 是靠 2/1–12/31 的费用比较才定的。
- 怎么补:**做实验 3**(1 月内部切分)。

**【16】`tab:p3-check` 里"跨日状态衔接"未通过天数 = 0,这个检查在哪段代码里?**
- 能否回答:❌ **不存在。这个 0 是补出来的。**
- 怎么补:删行,或改为"由逐日 E₀ 传递结构性保证"。

**【17】P4 的三个策略是"各自独立标定"的吗?**
- 能否回答:❌ 代码 docstring 明说三个模式**共用同一组 1 月预热期参数**。
- 怎么补:改为"评价区间内逐策略独立重标定;1 月预热参数三者共用并冻结,以保证 ΔJ 只反映评价区间内的价格信息差"。

**【18】你声称缓存三元组隔离"已校验",在哪个函数里?**
- 能否回答:❌ **没有这个自检。**
- 怎么补:要么删掉这句,要么真写一个自检(比较相同键的缓存命中与重算结果)。

**【19】"下调量恒为零"你怎么证明?**
- 能否回答:⚠️ 你先用 `ub = 0` 禁掉了下调量,再检查"有没有下调量"——**循环论证**。
- 怎么补:改成"主口径下把下调量上界置零是制度设定的直接实现;真正的验证是**放开该上界后 LP 是否仍返回零**"。跑一次放开版,若仍为零,才是真验证。

**【20】你的 4-3 主策略用了全部三个预报时刻,但你自己说 12:00 可以省去,为什么不省?**
- 能否回答:⚠️ 部分。题目给定三个时刻的预报,"主策略"定义为全部使用是合理的;组合研究回答的是假想问题。
- 怎么补:**明确写出来**。"主策略使用题目给定的全部三个时刻;若允许精简订阅,应选 {6:00, 18:00}"。摘要的"可以省去"也要改成"若需精简可省去"——否则评委会以为你明知更优解却没用。

**【21】你的 4-3 超参数 (α,ρ) 全部落在搜索网格的角落,这组参数有意义吗?**
- 能否回答:✅ 论文**主动承认了**"其取值不具可解释性"。
- 补充:这是加分的诚实,但**应该顺手把网格往外扩一圈**,否则等于说"我的最优解没找到"。

**【22】46 ms 的求解时间是怎么测的?**
- 能否回答:❌ 交付代码里没有单次求解的计时器,我实测均值 37.4 ms。
- 怎么补:要么补一个计时脚本,要么改成"量级为数十毫秒"。

**【23】你的预测误差 0.0446 元/kWh 对应多大的经济代价?**
- 能否回答:✅ 能。oracle − forecast = 94,145 元(4-2)/ 108,477 元(4-3) 正是这个代价。
- 补充:**这是全文最有说服力的一处量化,但摘要里没写。** 应该提上去。

**【24】如果现实中价格有真正的随机成分(机组停运、极端天气),你的模型会怎样?**
- 能否回答:✅ 能,§sec:p4-limit 明确讨论了边界。
- 补充:但**只做了定性讨论,没做压力测试**。可考虑补一个"预测误差放大 2 倍"的鲁棒性实验。

---

## 十、国奖修改清单

> 只列最重要的。按"不改会不会掉奖"排序,不按工作量。

---

### 🔴 P0 —— 不改可能直接影响获奖等级

---

**【P0-1】提交的 PDF 交叉引用全部失效(152 处 `??`)**

- **严重程度**:🔴 致命
- **为什么影响国奖**:`??` 分布在**连续 19 页**(第 3、9–12、15–21、23–41…)。"如表 `??` 所示""见附录第 `??` 节"反复出现,评委在速读的前 10 页就会形成"这队没检查过自己的论文"的判断。这一条会把你从国二拉到省奖。
- **具体怎么改**:**重新编译即可。** 根因是 `main.aux` 被一次中断的编译截断(704 字节、零条 `\newlabel`),而提交的 PDF 正是用这份残缺 aux 生成的。
- **建议增加什么内容**:在仓库加一个编译脚本,固定跑三遍 `xelatex + xdvipdfmx`;提交前用脚本统计 PDF 里的 `??` 数量,必须为 0。
- **修改后的目标**:**`??` = 0,页数仍为 144(正文 30 页不变)。** 我已实测验证:干净重编译后 `??` 由 152 → **0**,页数不变。

---

**【P0-2】交付脚本跑不出论文数字(P2 需未记载的 `--core jia`)**

- **严重程度**:🔴 致命
- **为什么影响国奖**:`format2026.doc` 第五条明确写"**结果与论文不符——可能被取消评奖资格**"。`python 03_代码/p2_microgrid.py` 的默认路径 `--core hybrid` 把 warmup 网格写死为 `alphas=(0.80,)`、`rhos=(0.75,)`,**产生不了** `α=0.75, ρ=0.9, λ=0.00` 和 180 点候选。
- **具体怎么改**:①把 `argparse` 的 `default` 改成 `"jia"`;②在 `03_代码/README.md` 和论文附录的支撑材料列表里写清复现命令;③把 `--core jia` 的来源(`solve2_final.py`)在文件头注明。
- **建议增加什么内容**:一个 `run_all.py`,按顺序跑出全部支撑材料,并打印每个头条数字的校验值。
- **修改后的目标**:**一条命令复现论文全部数字**,评委照 README 做能得到完全相同的结果。

---

**【P0-3】P3 头条数字的来源文件不存在,且两套实现"主口径"定义相反**

- **严重程度**:🔴 致命
- **为什么影响国奖**:`p3_rolling_microgrid.py` 头部声明产出 `p3_results.json`/`p3_cache/`,**这四个路径全都不存在**,该文件从未跑完过。同时滚动文件里 `main` = **不退款**,而论文主口径是**退款**。评委按文件头复现,会得到一个制度设定就不同的账本。
- **具体怎么改**:①头部改成"本文件仅供问题四复用内核,不作为问题三结果来源";②`"main"/"refund"` 改名为 `"norefund"/"refund"`;③在论文或 README 里写明 P3 真实产出链是 `p3_microgrid.py` → `p3_backtest.py` → `p3_analysis.py`。
- **建议增加什么内容**:在 `03_代码/` 加一份 `PIPELINE.md`,画清每个问题"哪个脚本 → 哪个文件 → 论文哪张表"。
- **修改后的目标**:评委可以逐个问题溯源,不存在"孤儿文件"和口径矛盾。

---

**【P0-4】检查表含"凭空的零"与空转项**

- **严重程度**:🔴 致命
- **为什么影响国奖**:`tab:p3-check` 的"跨日状态衔接 = 0"**在该链路里根本没有这个检查**;`tab:p4-check` 有两列零是 `.get(k, 0)` 补的;P4 的"信息边界"检查**不验任何发布时刻**;"下调恒为零"是循环论证。论文还把这些写进了**摘要**("六项强制检查全部通过")。国奖评委会翻附录。
- **具体怎么改**:①删除或改写不存在的检查行;②`.get(k,0)` 改为 `---`;③"信息边界"改名为"数值有效性",或真写一条能被违反的发布时刻断言;④"下调恒为零"改为"放开上界后仍返回零"的真验证;⑤把"同时充放电"从"结构上恒成立"改标为"真实检验"(它是纯 LP 链路里唯一的保证,被你们说轻了)。
- **建议增加什么内容**:在检查表加一列"**判据类型**",取值 ∈ {独立复核, 构造性保证}。
- **修改后的目标**:**每一行检查都能被违反。** 摘要改为"六项检查通过,其中 2 项为独立复核"。

---

### 🟠 P1 —— 明显影响一等奖概率

---

**【P1-1】三个对照策略未同条件运行**

- **问题**:P2 的三个基线在不同的一月参数、不同 `CalConfig`、不同 `day0` 下运行,进入窗口时初始储电量 2287.68 / 7097.30 / 6000.0 kWh。
- **为什么影响国奖**:"较预测均值节省 6.47%"是摘要里的核心卖点之一。评委问一句"基线是同条件跑的吗",整个对照实验的可信度就塌了。
- **具体怎么改**:统一传入同一 `warmup`、同一 `CalConfig`、同一 `day0`;报告**期初**储电量;如实更新数字。
- **建议增加什么内容**:在 `tab:p2-strategy` 加一列"进入窗口时储电量/kWh"。
- **目标**:三个基线在完全相同的初始条件下对比,结论不可攻击。

**【P1-2】λ=0 使储备机制退化,论文解读的不是实际机制**

- **问题**:λ=0 → ξ 钉死在 4800 → Ē 在 15.6% 时段触上限、18.2% 触下限 → ρ 标定在退化轨迹上 → 执行层在 5.5% 时段被结构性禁止放电。
- **为什么影响国奖**:这是"模型里有个部件,但它没在按你描述的方式工作"。属于国奖级追问。
- **具体怎么改**:**做实验 2**(λ ∈ {0, 0.6, 1.2} 消融 + Ē 分布 + R_t 触发率)。
- **建议增加什么内容**:一张 λ 消融双轴图。
- **目标**:把"机制有没有用"从假设变成受控结论。

**【P1-3】P3 超参选择泄漏评价区间**

- **问题**:"评价区间内不再回头修改"不成立,中心点与 λ 由 2/1–12/31 费用选定。
- **为什么影响国奖**:方法论的干净程度是国一和省一的分水岭。
- **具体怎么改**:**做实验 3**(1 月内部 20/11 切分)。
- **目标**:参数选择全程不接触评价集。

**【P1-4】图 p3_fig4 内嵌标题"九种"与图注/正文"八种"矛盾**

- **问题**:同一张图,内嵌标题 9,LaTeX 标题 8,横轴 8 根柱。{6,12,18} 的子集恰为 2³=8。
- **为什么影响国奖**:评委数柱子是本能。这类低级矛盾会让人怀疑其他数字。
- **具体怎么改**:改 `p3_figures.py` 里的标题字符串,重新生成图。
- **目标**:图题、内嵌标题、横轴三者一致。

**【P1-5】交付代码缺少溯源与文档**

- **问题**:46 ms 不可复现;1.58× / 0.888 / 8.9% / 38.5% 等数字不由任何脚本产出;附录称 365 天但循环 364 次。
- **具体怎么改**:补计时脚本;把这些数字的计算并入对应脚本或明确标注出处;修正 364/365。
- **目标**:论文里每一个数字都能指到一段代码。

---

### 🟡 P2 —— 可进一步提高质量

| # | 问题 | 怎么改 |
|---|---|---|
| P2-1 | 论文说变量是 kWh,代码是 kW | 改 §5.2 表述 |
| P2-2 | LP 松弛描述与实际不符 | 改为"删除 z 与互斥约束的松弛",并说明它更强 |
| P2-3 | 系数集描述错误(实际含 ±5000) | 改正 |
| P2-4 | §5.4 夜间购电叙述与 `p1_detail.csv` 矛盾 | 按真实数据重写 |
| P2-5 | P3 锚点"实测功率" vs 代码"上一版预报" | 改为"最近一次发布的预报值" |
| P2-6 | P4"三种策略各自独立标定"与 docstring 冲突 | 改为"评价区间内独立、预热期共用" |
| P2-7 | 缓存隔离"已校验"但无自检 | 补自检或删句 |
| P2-8 | 5 张 P4 图未使用 | 用上 `p4_fig2_forecast`、`p4_fig4_purchase` |
| P2-9 | 缺三层架构总图 | **补一张**(性价比最高的图) |
| P2-10 | 摘要未提附件4价格结构发现 | 补一句 |
| P2-11 | 摘要"可以省去"措辞过强 | 改"若需精简可省去" |
| P2-12 | 分位数约定混用(线性插值 vs inverted_cdf) | 统一并说明 |
| P2-13 | 误差分位数阈值 10 vs 代码 8 | 统一 |
| P2-14 | 4-3 的 (α,ρ) 落在网格角落 | 网格外扩一圈重跑 |

---

### 🟢 P3 —— 格式/语言/排版

| # | 问题 | 怎么改 |
|---|---|---|
| P3-1 | λ、β、τ、⁰ 在 `lmmono9-regular` 缺字(128+ 处) | `\texttt{}` 里的希腊字母改用 `\ensuremath{}` 或换等宽字体 |
| P3-2 | 图 p2_fig2/p2_fig6 电价红线与紧急购电红柱撞色 | 紧急购电改橙色或紫色 |
| P3-3 | 图 p2_fig1/p2_fig3/p3_fig1 子图 y 轴量程不一致 | 统一量程 |
| P3-4 | y 上限 10000 但约束是 10800 | 轴上限设为 11000 |
| P3-5 | 部分子图缺上限/下限/Ē^tar 标注 | 补齐 |
| P3-6 | 正文顶格 30 页,零余量 | 精简冗余段落腾出 1–2 页缓冲 |
| P3-7 | `for i, row in enumerate(s["table1"]): pass` 死代码 | 删除 |
| P3-8 | `E_CAP = 12000.0` 定义但从未使用 | 删除或真正启用 |

---

### 国奖冲刺 Top 10

只做这 10 件。按顺序。

| # | 事项 | 工作量 | 为什么是这 10 件之一 |
|---|---|---|---|
| **1** | **重新编译,把 152 处 `??` 清零** | 5 分钟 | 最高性价比。不做,前面所有工作都白搭 |
| **2** | **改 `p2_microgrid.py` 默认核 + 写 README 复现命令** | 30 分钟 | 直接规避"取消评奖资格"条款 |
| **3** | **修正 `p3_rolling_microgrid.py` 头部声明与 `main/refund` 命名** | 20 分钟 | 消除 P3 口径矛盾与孤儿文件 |
| **4** | **重做检查表:删凭空的零、标出构造性保证、区分独立复核** | 2 小时 | 摘要里那句"全部通过"现在是最大的风险点 |
| **5** | **实验 1:三个基线同条件重跑 + 报告期初储电量** | 半天 | 堵死"对照不公平"这个必问项 |
| **6** | **实验 2:λ 消融 + Ē 退化率 + R_t 触发率** | 半天 | 把"机制空转"从硬伤变成受控结论 |
| **7** | **修 §5.4 夜间购电叙述 + LP松弛描述 + 系数集描述** | 1 小时 | 三处"文字与自己的数据/代码矛盾",评委一查就中 |
| **8** | **改图 p3_fig4 标题"九种"→"八种"并重新生成** | 10 分钟 | 同一张图内自相矛盾,最伤印象分 |
| **9** | **补一张三层滚动架构总图** | 2 小时 | 全文最关键的方法论目前只有文字,速读评委看不见 |
| **10** | **实验 3:P3 超参按 1 月内部 20/11 切分重选** | 半天 | 消除唯一一处超参评价集泄漏 |

**外加一件免费的**:把 `04_图/` 里没用上的 `p4_fig2_forecast` 和 `p4_fig4_purchase` 插进 P4 章节。图已经画好,白扔了可惜,还能顺带修掉 P2/P4 图数失衡(6:2 → 6:4)。

---

## 附:我无法判定、需要你提供才能验证的事项

诚实起见,以下几条我没有下结论:

1. **46 ms 求解时间** —— 我只测到均值 37.4 ms(范围 37–56)。论文的 46 ms 在合理区间内,但**无法从交付代码复现**。需要你提供测量脚本或原始记录才能确认。
2. **P3 的 11.44 万边际值** —— 摘要说它来自问题三(固定电价)。`p3_analysis.json` 里没有"组合"键,我在 P4 的 JSON 里找到了对应的负数(−6.23 万)。**P3 的 8 组合表在哪个文件里,需要你指给我**,我才能确认 11.44 万。
3. **λ=0 对最终结论的净影响** —— 我确认了 Ē 退化与 R_t 约束在 5.5% 时段触发,但**"这 5.5% 值多少钱"必须跑消融才能算**。不做实验 2,这个问题无人能答。
4. **4-3 网格外扩后的真实最优** —— (α,ρ) 落在角落意味着真最优在网格外。需要扩大搜索才能判定现有 14,402,403.87 是不是真的最优。

---

**最后一句实话。**

这份论文的**底层工作是国一水平的**:前视偏差被我用投毒实验证伪了(这是罕见的严格)、附件4 的价格结构分解是真发现、P4 的 12 条边际值逐个精确复现、关键比 0.8 的理论-数据互证很漂亮。**你们不缺建模能力。**

你们缺的是**收尾的纪律**——152 处 `??`、凭空的零、与代码不符的描述、不同条件的基线。这些每一项单独看都是小事,**合起来正好是国二和国一的分界线**。

上面的 Top 10,全部做完大概两天。做完之后,这份论文进国一答辩是有底气的。
